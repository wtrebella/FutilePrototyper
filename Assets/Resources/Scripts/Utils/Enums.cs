public enum SideType {
	Top,
	Right,
	Bottom,
	Left
}

public enum TableCellType {
	Title,
	Item,
	Done
}

public enum ScrollType {
	Horizontal,
	Vertical,
	Both
}

public enum ComponentType {
	None,
	Sprite,
	SliceSprite,
	ScrollBar,
	Label,
	Touch,
	RadialWipeSprite,
	ScrollContainer,
}